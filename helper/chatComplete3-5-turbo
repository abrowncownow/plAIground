export async function get_completion(prompt, model="gpt-3.5-turbo") {
  const messages = [{"role": "user", "content": prompt}];
  const response = await openai.ChatCompletion.create({
    model: model,
    messages: messages,
    temperature: 0
  });
  return response.choices[0].message["content"];
}